# Nginx template for lawyer.mela-media.co.il multi-tenant SPA + api-lawyer backend.
# Install on frontend VPS (84.46.253.85) and backend VPS (37.60.230.148).

# --- Frontend: /etc/nginx/sites-available/lawyer.mela-media.co.il ---
# server {
#     listen 443 ssl http2;
#     server_name lawyer.mela-media.co.il;
#     root /var/www/lawyer;
#     index index.html;
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
# }

# --- Backend: /etc/nginx/sites-available/api-lawyer.mela-media.co.il ---
# upstream lawyer_api {
#     server 127.0.0.1:3005;
# }
# server {
#     listen 443 ssl http2;
#     server_name api-lawyer.mela-media.co.il;
#     location / {
#         proxy_pass http://lawyer_api;
#         proxy_http_version 1.1;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }

# PM2 (backend 37.60.230.148):
#   cd /root/LawyerPlatform && git checkout Melamedia && git pull
#   MULTI_TENANT_MODE=true PORT=3005 pm2 start backend/server.js --name lawyer-api
#   pm2 save
#
# Postgres: create DB `lawyer` and run migrations.
#
# Register in central-platform .env:
#   LAYERWEBSITES_TENANT_ADAPTERS=lawyer|https://api-lawyer.mela-media.co.il|https://lawyer.mela-media.co.il/admin/master
