# Frontend production env example (CRA)
# Used at build time.

# API base URL for production (no trailing slash recommended)
# Example: https://api.calls.melamedlaw.co.il/api
REACT_APP_API_BASE_URL=__SET_ME__
