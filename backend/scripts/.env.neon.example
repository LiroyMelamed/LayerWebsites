# Copy to backend/scripts/.env.neon (gitignored) — NEVER commit real credentials.
#
# After Neon password rotation, paste the new password or full connection string here.

# Option A — full URL from Neon dashboard (Connect → pooled):
# NEON_DATABASE_URL=postgresql://neondb_owner:YOUR_NEW_PASSWORD@ep-....pooler.neon.tech/neondb?sslmode=require

# Option B — password only (uses default host/user/db from sync script):
NEON_PASS=YOUR_NEW_PASSWORD
